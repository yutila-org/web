---
// src/pages/software/camelot.astro
import '../../styles/camelot-docs.css';
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Camelot Framework</title>
    </head>
    <body>
        <header>
            <a href="/" style="text-decoration: none; color: inherit;"><strong>Camelot</strong></a>
            <nav>
                <a href="/software">Back to Software</a> | 
                <a href="https://github.com/yutila-org/camelot">GitHub</a> | 
                <a href="/docs/camelot">Read the Docs</a>
            </nav>
            <hr />
        </header>

        <main>
            <h1>Camelot C Framework</h1>
            
            <p>
                Camelot is a C framework for when you just want predictable memory behavior and no surprises.
                It uses memory arenas instead of random malloc calls, enforces standard C23 rules, and uses its own build engine (Merlin) instead of traditional makefiles or complex build scripts.
            </p>

            <pre><code>#include &lt;camelot.h&gt;

int main(void) &#123;
    // Create an arena
    Arena *arena = ArenaInit(NULL, MEGABYTES(128));

    // Allocate without worrying about freeing individual blocks
    void *block = ArenaAlloc(arena, 1024);

    ArenaFree(&arena);
    return 0;
&#125;</code></pre>

            <h2>Why use it?</h2>
            
            <ul>
                <li><strong>Merlin Build Engine:</strong> A standalone build tool written in D. It handles GCC flags, sanitizers, and compilation without messy build scripts.</li>
                <li><strong>No Malloc:</strong> We use memory arenas for everything. This means fast, predictable allocations and no memory leaks.</li>
                <li><strong>Hardened by Default:</strong> Automatically compiles with stack protectors, PIE, and ASAN/UBSAN to catch common C bugs early.</li>
            </ul>

            <h2>Get Started</h2>
            
            <p>You can clone and build it directly. No extra dependencies are needed.</p>

            <pre><code>git clone https://github.com/yutila-org/camelot
make</code></pre>

        </main>

        <footer>
            <hr />
            <p>
                Released under the <a href="https://www.mozilla.org/en-US/MPL/2.0/">MPL 2.0</a>. 
                <a href="https://www.bestpractices.dev/projects/12919">OpenSSF Certified</a>.
            </p>
        </footer>
    </body>
</html>
