name: Stage 3 DAST
on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  dast:
    uses: yutila-org/.github/.github/workflows/stage3-dast.yml@main
    with:
      target_url: "https://yutila.com"
